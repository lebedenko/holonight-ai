# Chat Header Selection Tasks

Status: Complete

- [x] Define explicit provider/model/status QML properties while retaining canonical `ModelId`.
- [x] Load defaults for all providers and implement session memory and refresh fallback policy.
- [x] Preserve conversation restoration against late refresh and route send/regenerate unchanged.
- [x] Replace `ModelPicker` with `HeaderComboBox` and `ChatHeader`.
- [x] Integrate full workspace and compact quick-panel layouts.
- [x] Recompose the header to match the mockup's inline selectors, textual status, and icon actions.
- [x] Add selected-provider Settings navigation and migrate the workspace inspector.
- [x] Update selection/routing/restoration tests.
- [x] Run focused tests, QML lint, build, full tests, and formatting checks.
- [x] Complete visual/manual acceptance on a graphical Wayland session.
